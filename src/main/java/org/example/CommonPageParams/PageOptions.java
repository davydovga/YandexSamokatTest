package org.example.CommonPageParams;

import org.openqa.selenium.WebDriver;

public class PageOptions {
    protected WebDriver driver;

    public PageOptions(WebDriver driver){
        this.driver = driver;
    }
}
